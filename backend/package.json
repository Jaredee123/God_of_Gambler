{
  "name": "backend",
  "version": "1.0.0",
  "description": "Express and MongoDB backend for the settlement game",
  "main": "src/server.js",
  "type": "module",
 "scripts": {
    "start": "node -r dotenv/config src/server.js",
    "dev": "nodemon --watch src --exec \"node -r dotenv/config\" src/server.js"
  },
  "keywords": [
    "express",
    "mongodb",
    "mongoose",
    "dotenv"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.6.1",
    "express": "^4.18.2",
    "mongoose": "^7.3.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
